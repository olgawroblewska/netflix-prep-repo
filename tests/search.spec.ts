import { test, expect } from '@playwright/test';

test.describe('Search functionality', () => {

    test.beforeEach(async ({page}) => {
        
    });

    test('Open search window', async ({ page }) => {


    });

});